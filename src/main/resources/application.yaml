spring:


  datasource:
    url: jdbc:mysql://localhost:3306/classicmodelsV2
    username: root
    password: Cmwende@3
    driver-class-name: com.mysql.cj.jdbc.Driver


    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8083
application:
  name: classicmodelsV2

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE